import searchIcon from '../../assets/icons/search.svg'
const SearchIcon = ({ className }) => {
  return <img src={searchIcon} alt='search-icon' className={className}></img>
}

export default SearchIcon
