import messageIcon from '../../assets/icons/message.svg'
const MessageIcon = ({ className }) => {
  return <img src={messageIcon} alt='share-icon' className={className}></img>
}

export default MessageIcon
