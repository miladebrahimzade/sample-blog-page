import suggestionIcon from '../../assets/icons/suggestion.svg'
const SuggestionIcon = ({ className }) => {
  return (
    <img src={suggestionIcon} alt='suggestion-icon' className={className}></img>
  )
}

export default SuggestionIcon
